Wrote `/Users/bill/Desktop/Desktop/q-periapt/docs/THREAT_MODEL.md` (the path the README already links). It is grounded in the actual tree state I verified (`Cargo.lock` has zero crypto backends; `crates/q-periapt-core/src/lib.rs` confirms no `C2PRI` const, no length validation on `CompatXWing` at line 168, `u64` length prefix at line 154, opaque `context` blob, `ToyXof` tests, no `implicit_reject`/`ct_select`).

Key honesty decisions, reflecting the blueprint positioning and the adversarial review:

- **Implementation-state tags** (✅/🟡/❌) on every mitigation, so the doc reads as design-vs-reality, not an assurance statement. Most cryptographic and CT mitigations are marked ❌ open because no backend is wired.
- **Real numbers, corrected per review**: X-Wing combiner = exactly 134 B (32+32+32+32+6) = 1 Keccak permutation; `ContextBound` >1.2 KB ≈ ~10 permutations expressed as "~1 vs ~10 SHA-3 blocks" (not the unsourced "1,774 vs 12,586 cycles / 7×"); the "8–10% encap/decap win" demoted to "low-single-digit-% at most, ML-KEM-dominated"; `Adv_C2PRI ≤ (q_g+q_j+2)/2^256`; SP 800-230 2^24 SLH-DSA cap.
- **All four CRITICAL findings** are first-class threats and flagged OPEN: missing C2PRI guard (§6.3), missing implicit rejection / `?`-early-return oracle (§6.4), `CompatXWing` length-ambiguity / 33+31-vs-32+32 collision (§6.8), non-functional downgrade floor + opaque-context binding (§6.7).
- **HIGH/MEDIUM findings** incorporated: per-backend non-uniform CT (drops "we audited KyberSlash" framing), CT-coverage-matrix instead of blanket binary-CT claim, ML-DSA rejection-sampling carve-out, FFI malformed-vs-invalid and diagnostic-string sub-oracles, `catch_unwind` requires `std`, X25519 all-zero no-abort, zeroization gaps, `u64`-vs-`u16/u32` spec drift, P99/cross-platform reframed from "win" to "measure honestly / reduced audit surface".
- **No unsubstantiated "faster"/"more secure"** anywhere; §7 enforces the parity-vs-binding-trade framing; out-of-scope explicitly excludes FIPS validation, HQC side-channel resistance, host compromise, fault injection, and simultaneous all-component break.