language = "C"
header = """/* PQ/T Hybrid Suite C ABI — auto-generated by cbindgen. Do not edit. */
/*
 * No aliasing (checked): within a single call, input (ptr,len) buffers and output (ptr,len)
 * buffers must not overlap (concurrent read/write of the same memory is undefined behavior).
 * The multi-buffer entry points CHECK the ranges and return Q_PERIAPT_ERR_ALIASING (-7) before
 * any access, so an overlap is a defined error, not UB. Pass distinct buffers regardless.
 */"""
include_guard = "Q_PERIAPT_H"
autogen_warning = "/* Generated; regenerate with: cbindgen --config cbindgen.toml --crate q-periapt-ffi --output include/q_periapt.h */"
pragma_once = true
tab_width = 4
style = "type"

[export]
prefix = ""
# Export the Q_PERIAPT_* constants and the q_periapt_* functions.
include = []

[defines]
